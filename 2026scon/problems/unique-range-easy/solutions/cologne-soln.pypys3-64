import sys
from itertools import pairwise

input = lambda: sys.stdin.readline().rstrip("\n")


def main():
    print(*((-i // 2 if i % 2 == 0 else (i + 1) // 2) for i in range(int(input()))))


if __name__ == '__main__':
    main()
