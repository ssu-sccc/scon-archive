#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[]) {
    registerValidation(argc, argv);

    int n = inf.readInt(1, 1'000'000, "N");
    inf.readSpace();
    int a = inf.readInt(1, 1'000'000, "A");
    inf.readSpace();
    int b = inf.readInt(n, 1'000'000, "B");
    inf.readEoln();
    inf.readEof();
}
