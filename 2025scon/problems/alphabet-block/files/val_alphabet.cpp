#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    const int MAX = 1'000'000;
    int nS = inf.readInt(0, MAX, "nS");
    inf.readSpace();
    int nC = inf.readInt(0, MAX, "nC");
    inf.readSpace();
    int nO = inf.readInt(0, MAX, "nO");
    inf.readSpace();
    int nN = inf.readInt(0, MAX, "nN");
    inf.readEoln();
    inf.readEof();
}